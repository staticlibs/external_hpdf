# Copyright 2017, alex at staticlibs.net
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

language: cpp

sudo: required

services:
  - docker

os:
  - linux
  - osx

before_install:
  - export CC=
  - export CXX=
  - export D="docker exec fedora"
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then
    docker pull fedora ;
    docker run -id --name fedora -v `pwd`/..:/src -w=/root fedora bash ;
    fi

install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then 

    `# Ubuntu` ;
    sudo apt-get -y install
        g++
        pkg-config
        cmake
        make
        valgrind
        zlib1g-dev
        libpng12-dev ; 

    `# Fedora` ;
    $D dnf install -y
        gcc-c++
        pkg-config
        cmake
        make
        valgrind
        zlib-devel
        libpng-devel ;

    fi

before_script:
  - git submodule update --init --recursive
  - cd ..
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then
    git clone --recursive https://github.com/staticlibs/external_zlib.git ;
    git clone --recursive https://github.com/staticlibs/external_libpng.git ;
    fi
  - git clone https://github.com/staticlibs/cmake.git
  - git clone https://github.com/staticlibs/staticlib_config.git

script:
  - set -e
  - mkdir build-standalone
  - mkdir build

  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then

    `# macOS` ;
    set +e ;
    pushd build-standalone ;
    set -e ;
    cmake ../external_hpdf ;
    make ;
    set +e;
    popd ;
    pushd build ;
    set -e ;
    cmake ../external_hpdf/test -DSTATICLIB_TOOLCHAIN=macosx_amd64_clang -DCMAKE_BUILD_TYPE=Release ;
    make all test ;

    fi

  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then

    `# Ubuntu` ;
    lsb_release -a ;
    pushd build-standalone ;
    cmake ../external_hpdf ;
    make ;
    popd ;
    pushd build ;
    cmake ../external_hpdf/test -DCMAKE_BUILD_TYPE=Release ;
    make all test ;

    `# Fedora` ;
    $D cat /etc/fedora-release ;
    $D cmake /src/external_hpdf ;
    $D make ;
    $D rm -rf * ;
    $D cmake /src/external_hpdf/test -DCMAKE_BUILD_TYPE=Release ;
    $D make all test ;

    fi

notifications:
  email:
    on_success: always
